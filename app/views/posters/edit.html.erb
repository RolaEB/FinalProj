<%= render 'userNavBar',user: current_user %>

<div class="container">
    <div class="card card-login mx-auto mt-5">
      <div class="card-header">Edit your Poster</div>
      <div class="card-body">

<%= render 'form', poster: @poster %>
<hr>
     
<div class="text-center">
<small>Want to delete?</small>
<br/>
<%= link_to "Delete Poster", @poster, data: { confirm: "Are you sure?" }, method: :delete,class:"btn btn-danger" %>
</div>

</div>
</div>
</div>

</div>
<br/>
<br/>
<%= render 'footer' %>
