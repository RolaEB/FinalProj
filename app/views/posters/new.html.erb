<%= render 'userNavBar',user: current_user %>

<div class="container">
    <div class="card card-login mx-auto mt-5">
      <div class="card-header">Create Lost/Found Poster</div>
      <div class="card-body">

<%= render 'form', poster: @poster %>

</div>
</div>
</div>
